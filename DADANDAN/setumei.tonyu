extends TextChar;
text="spaceキーで操作説明";
font="ＭＳ 明朝";
mode=0;
y=372;
while(mode==0) {
  if (getkey(32)==1) mode=1;
  update();
}
text="Ａキーで各ステージに移動。メニューに戻ってくるのもＡキー。メニューでは、上下キーでステージセレクト、左右キーで難易度変更です。各ステージでは、ｚキーでショット、ｘキーでボム。ボムは初期状態で３つ装備されてます。画面にCRASHと出た時にｘキーを押すと、喰らいボムを出せます。ただし、通常ボム二つ分使用します。十字キーで移動、Ｓｈｉｆｔキー押しながらで低速移動です。";
x=608;
while(1) {
  x-=3;
  if (getkey(90)>0) x-=6;
  if (x<-5026) x=608;
  update();
}